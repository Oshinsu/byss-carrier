/**
 * Re-export useToast from the toast UI component.
 * Usage: const { toast } = useToast();
 *        toast("Message", "success");
 *        toast("Erreur", "error", { title: "Oups" });
 */
export { useToast } from "@/components/ui/toast";
